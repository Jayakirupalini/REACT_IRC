import { Button } from '@mui/material';
import React from 'react';

export const Home = () => {
  return (
    <div>
        <Button on variant="contained">Contained</Button>
    </div>
  )
}
